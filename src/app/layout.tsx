export const metadata = {
  title: "Task Board App",
  description: "A sustainable fashion rental marketplace.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

